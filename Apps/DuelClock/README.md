# 对弈钟

在 Xcode 27.1 中打开 `DuelClock.xcodeproj`，选择 iPhone Duo 模拟器运行。

主要文件：`Sources/MatchClock.swift` 管理回合、计时、暂停和重置；`Sources/DuelClockApp.swift` 负责界面与分区。点击任一侧开始；正在计时的一侧再次点击后交棒。布局设置可以关闭适配或面对面方向。

返回 [合集说明](../../README.md)。
